<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://jsrdzhk.github.io</id>
    <title>Rodney Cheung&apos;s House</title>
    <updated>2021-02-19T08:27:10.331Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://jsrdzhk.github.io"/>
    <link rel="self" href="https://jsrdzhk.github.io/atom.xml"/>
    <subtitle>The king is noble only for practicing his way😄!</subtitle>
    <logo>https://jsrdzhk.github.io/images/avatar.png</logo>
    <icon>https://jsrdzhk.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, Rodney Cheung&apos;s House</rights>
    <entry>
        <title type="html"><![CDATA[写给开发者Kotlin指引（二）]]></title>
        <id>https://jsrdzhk.github.io/post/xie-gei-kai-fa-zhe-kotlin-zhi-yin-er/</id>
        <link href="https://jsrdzhk.github.io/post/xie-gei-kai-fa-zhe-kotlin-zhi-yin-er/">
        </link>
        <updated>2021-02-19T08:20:30.000Z</updated>
        <content type="html"><![CDATA[<p><a href="/post/xie-gei-kai-fa-zhe-kotlin-zhi-yin-yi">上一篇</a>说明了<code>Kotlin</code>的基本语法，本节将深入讲解lambda函数，这是<code>Kotlin</code>中使用最频繁的进阶技巧。</p>
<h2 id="一-lambda函数">一、Lambda函数</h2>
<h3 id="11-基本用法">1.1 基本用法</h3>
<p>Lambda函数对于<code>C++</code>开发者或者<code>Python</code>不用多解释了，<code>Java</code>目前也加入了<code>Lambda</code>的支持。简单来说，Lambda函数就是匿名函数，他可以将函数当成参数传入其他的函数调用中。在C语言中，通过函数指针来实现了这一功能，而在C++中，除了兼容C的函数指针外，还引入了<code>std::function</code>的标准库支持，另外在Modern C++中还加入了lambda的支持。得益于C++丰富的语意支持，lambda函数的功能异常的丰富，但是也加大了使用的难度。</p>
<p>在<code>Kotlin</code>中，lambda函数的语法非常简单：</p>
<pre><code class="language-kotlin">{param1:param1Type param2:param2Type -&gt; functionImplementation}
</code></pre>
<p>现在通过一个例子说明lambda函数的用法，假设我们需要找到一个字符串数组中长度最大的一个字符串:</p>
<pre><code class="language-kotlin">fun sortArray(): String? {
    val list = listOf(&quot;zaabb&quot;, &quot;aa&quot;, &quot;aaazz&quot;)
    val lambdaFunc = { s: String -&gt; s.length }
    return list.maxByOrNull(lambdaFunc)
}
</code></pre>
<p>这里仔细讲解一下，该函数的返回值是<code>String</code>，后面的<code>?</code>表示该函数可能返回一个<code>null</code>值，<code>listOf</code>的定义如下:</p>
<pre><code class="language-kotlin">public fun &lt;T&gt; listOf(vararg elements: T): List&lt;T&gt; = if (elements.size &gt; 0) elements.asList() else emptyList()
</code></pre>
<p>它是一个泛型函数，我们在调用时需要提供一个类型<code>T</code>，该类型就是<code>listOf</code>函数的参数类型，<code>vararg</code>表示变长参数，我们可以在这里提供多个<code>T</code>类型的参数。我们在调用时的标准写法应该是<code>listOf&lt;String&gt;(&quot;zaabb&quot;, &quot;aa&quot;, &quot;aaazz&quot;)</code>，但是泛型函数拥有推断能力，由于我们提供的三个参数都是<code>String</code>类型，所以在调用时可以省略<code>&lt;&gt;</code>。该函数的函数体很简单，就是将边长参数的所有<code>elements</code>转化为一个<code>List&lt;T&gt;</code>。</p>
<p><code>List&lt;T&gt;</code>拥有一个<code>maxByOrNull</code>的方法，来看它的定义：</p>
<pre><code class="language-kotlin">@SinceKotlin(&quot;1.4&quot;)
public inline fun &lt;T, R : Comparable&lt;R&gt;&gt; Iterable&lt;T&gt;.maxByOrNull(selector: (T) -&gt; R): T? {
    val iterator = iterator()
    if (!iterator.hasNext()) return null
    var maxElem = iterator.next()
    if (!iterator.hasNext()) return maxElem
    var maxValue = selector(maxElem)
    do {
        val e = iterator.next()
        val v = selector(e)
        if (maxValue &lt; v) {
            maxElem = e
            maxValue = v
        }
    } while (iterator.hasNext())
    return maxElem
}
</code></pre>
<p>这是一个内联函数，这个概念在C++中同样存在，就是会将函数在调用处完全展开（C++也不一定会完全展开，编译器会做一些判断，不过用户可以通过一些编译器选项来强制展开）。<code>&lt;T, R : Comparable&lt;R&gt;&gt;</code>指定了该函数在调用时需要提供哪些类型，而<code>R : Comparable&lt;R&gt;</code>表示第二个类型<code>R</code>的类型必须是<code>Comparable&lt;R&gt;</code>的子类。<code>Iterable&lt;T&gt;.</code>表示<code>maxByOrNull</code>函数是<code>Iterable&lt;T&gt;</code>的一个扩展函数。扩展函数是<code>Kotlin</code>中的一个新概念，旨在减少<code>Util</code>类的使用。因为<code>Iterable&lt;T&gt;</code>本身没有定义<code>maxByOrNull</code>方法，所以只要在这里补充定义一下，那么就等于<code>Iterable&lt;T&gt;</code>中定义了一个<code>public</code>的成员方法。接下来分析该函数的参数，<code>selector</code>的类型看上去跟<code>lambda</code>函数的语法非常相似，其实确实是这样，括号中表示该函数接受的参数，为<code>T</code>,而<code>R</code>则表示该函数的返回类型，也就是一个<code>Comparable&lt;R&gt;</code>对象。最后的返回类型是<code>T?</code>，表示返回的可能是<code>T</code>类型的变量，也有可能是个<code>null</code>。</p>
<p>在分析函数体之前，先想一下为什么<code>List&lt;T&gt;</code>类型的对象能够调用<code>Iterable&lt;T&gt;</code>的扩展函数<code>maxByOrNull</code>？根据我们对面向对象的理解，猜测<code>List</code>应该是<code>Iterable</code>的子类，通过追踪<code>Kotlin</code>源代码，发现<code>List</code>继承于<code>Collection</code>类，而<code>Collection</code>类继承于<code>Iterable</code>类，说明<code>List</code>是可迭代的。</p>
<p>下面看函数体，第一行调用了<code>iterator()</code>,它的定义是：<code>public operator fun iterator(): Iterator&lt;T&gt;</code>，用于返回一个可迭代对象的迭代器，<code>operator</code>用于标记该函数是一个可重载的运算符函数，这里暂时不细说。<code>iterator</code>有几个成员函数，用于在可迭代对象中移动迭代器。其中<code>hasNext</code>用于判断迭代器是否挪到了终点，而<code>next</code>则指使迭代器挪到可迭代对象中的下一个元素上。这种模式在C++中经常在代码层使用，所以C++开发者应该能够轻易理解这里的意思。不理解的，可以认为就是一个for循环对于<code>List</code>的遍历。了解了这些细节之后，函数的逻辑就很好理解了，就是通过遍历<code>List</code>中的所有元素，并通过<code>selector</code>函数将这些元素都转化为<code>Comparable&lt;R&gt;</code>对象，然后使用<code>&lt;</code>来比较他们的大小，并将大的暂时存储到一个临时变量中，整个算法的复杂度是<code>O(N)</code>。顺便提一下，<code>Comparable&lt;R&gt;</code>中的<code>&lt;</code>号也是一个<code>operator</code>，这就涉及到运算符重载的问题，C++开发者应该能很快领悟其中的奥妙。</p>
<h3 id="12-简化调用">1.2 简化调用</h3>
<p>其实<code>lambda</code>函数的调用并不需要先声明出来，所以，上面的代码可以简化为:</p>
<pre><code class="language-kotlin">fun sortArray(): String? {
    val list = listOf(&quot;zaabb&quot;, &quot;aa&quot;, &quot;aaazz&quot;)
    return list.maxByOrNull({ s: String -&gt; s.length })
}
</code></pre>
<p><code>Kotlin</code>还规定，当函数的参数的最后一个参数是函数时，可以将函数参数挪到外面：</p>
<pre><code class="language-kotlin">fun sortArray(): String? {
    val list = listOf(&quot;zaabb&quot;, &quot;aa&quot;, &quot;aaazz&quot;)
    return list.maxByOrNull(){ s: String -&gt; s.length }
}
</code></pre>
<p>当函数只有一个参数，且该参数是函数时，可以省略调函数调用符号，也就是括号：</p>
<pre><code class="language-kotlin">fun sortArray(): String? {
    val list = listOf(&quot;zaabb&quot;, &quot;aa&quot;, &quot;aaazz&quot;)
    return list.maxByOrNull{ s: String -&gt; s.length }
}
</code></pre>
<p>由于类型推导的存在，我们同样也可以省略<code>s</code>后面的参数类型:</p>
<pre><code class="language-kotlin">fun sortArray(): String? {
    val list = listOf(&quot;zaabb&quot;, &quot;aa&quot;, &quot;aaazz&quot;)
    return list.maxByOrNull{ s -&gt; s.length }
}
</code></pre>
<p>最后，如果作为参数的函数签名只有一个参数，那么连前面的参数名都可以省略，直接用<code>it</code>代替即可：</p>
<pre><code class="language-kotlin">fun sortArray(): String? {
    val list = listOf(&quot;zaabb&quot;, &quot;aa&quot;, &quot;aaazz&quot;)
    return list.maxByOrNull{ it.length }
}
</code></pre>
<h2 id="二-总结">二、总结</h2>
<p><code>lambda</code>函数大大简化了开发中的冗余代码，而<code>Kotlin</code>的简洁高效也开始初步展现。(C++:什么叫编程语言的皇冠啊😄)</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[写给开发者Kotlin指引（一）]]></title>
        <id>https://jsrdzhk.github.io/post/xie-gei-kai-fa-zhe-kotlin-zhi-yin-yi/</id>
        <link href="https://jsrdzhk.github.io/post/xie-gei-kai-fa-zhe-kotlin-zhi-yin-yi/">
        </link>
        <updated>2021-02-19T07:57:13.000Z</updated>
        <content type="html"><![CDATA[<p>本文从开发者学习第二门语言的角度，力求用最简洁的语言说明<code>Kotlin</code>的用法。在你花10分钟阅读完后，应该可以写出不那么Kotlin的Kotlin代码。</p>
<h2 id="一-和java的关系">一、和Java的关系</h2>
<blockquote>
<p>Kotlin is a cross-platform, statically typed, general-purpose programming language with type inference. Kotlin is designed to interoperate fully with Java, and the JVM version of Kotlin's standard library depends on the Java Class Library, but type inference allows its syntax to be more concise.</p>
</blockquote>
<p>在维基百科中说明了<code>Kotlin</code>是可以与Java进行完全互操作的语言。事实上，Kotlin的编译器跟Java的编译器一样，都是将代码转化为了JVM能够理解的字节码。类似于C++语言先通过编译器编译成汇编语言，汇编语言再由汇编器生成了机器码，最后再由链接器链接为可执行文件。</p>
<h2 id="二-函数">二、函数</h2>
<p>当提及到一门语言，函数和变量是必须的，直接从一个简单的函数来看。</p>
<pre><code class="language-kotlin">fun defaultArguments(param1: Int = 1, param2: String = &quot;asa&quot;, param3: Double = 3.3): Unit {
    println(&quot;$param1,$param2,$param3&quot;)
}

fun namedArguments(param1: Int = 1, param2: String = &quot;asa&quot;, param3: Double): Unit =
    println(&quot;$param2,${param1 + param3}&quot;)
</code></pre>
<p><code>Kotlin</code>的函数定义非常简单，以<code>fun</code>作为函数的标记，对于函数的形参，使用后置类型的方式来说明形参的类型，它还支持默认参数，函数的返回类型在形参之后使用冒号来说明，<code>Unit</code>相当于一般语言中的<code>void</code>，表示函数的返回类型没用。函数体类似于大部分语言，使用大括号来表示，如果函数体为表达式（表达式的概念现在暂时不管，直接使用大括号即可），也可以像声明变量那样用<code>=</code>。</p>
<p>默认参数的用法很简单，你可以选择在任意参数开始使用默认参数，例如<code>defaultArguments()</code>或者<code>defaultArguments(3,&quot;as&quot;)</code>，这没什么好说的。具名参数（named argument）是<code>Kotlin</code>中的一个特色（用过<code>Python</code>的人可以跳过这一部分，因为没什么区别），他允许函数调用者指定对应参数的值，配合默认参数，会让函数的调用更加灵活。例如，上面的<code>namedArguments</code>的函数可以这样调用:</p>
<pre><code class="language-kotlin">namedArguments(param3 = 1.3)
</code></pre>
<p>因为<code>namedArguments</code>只给前两个参数设置了默认参数，所以如果没有具名参数，那么就得给出所有参数。在使用具名参数后，函数的形参可以用更加合理的方式来排布顺序，而不需要为了配合默认参数调整形参的顺序。</p>
<p>OK，现在让我们运行一下上面的代码看看效果吧先给出完整的<code>main.kt</code>文件：</p>
<pre><code class="language-kotlin">fun defaultArguments(param1: Int = 1, param2: String = &quot;asa&quot;, param3: Double = 3.3): Unit {
    println(&quot;$param1,$param2,$param3&quot;)
}

fun namedArguments(param1: Int = 1, param2: String = &quot;asa&quot;, param3: Double): Unit =
    println(&quot;$param2,${param1 + param3}&quot;)
    
fun main(args: Array&lt;String&gt;) {
    defaultArguments(1)
    defaultArguments(1, &quot;as&quot;)
    namedArguments(param3 = 1.3)
}
</code></pre>
<p><code>Kotlin</code>的入口函数为<code>main</code>函数，想要运行Kotlin代码，先要将其编译成jvm虚拟机能够识别的字节码，<code>Kotlin</code>为我们提供了编译器，在Mac OS平台上，可以直接使用<code>brew install kotlin</code>来安装<code>Kotlin</code>的编译套件，其他平台的安装参考<a href="https://kotlinlang.org/docs/tutorials/command-line.html">这里</a>。</p>
<p>使用以下命令来编译代码为<code>jar</code>文件：</p>
<pre><code>kotlinc main.kt -include-runtime -d main.jar
</code></pre>
<p>然后，类似于<code>Java</code>，直接使用<code>java -jar main.jar</code>来运行，看一下结果：</p>
<pre><code>1,asa,3.3
1,as,3.3
asa 2.3
</code></pre>
<p>上述的代码中还包括一个非常实用的字符串处理技巧，字符串格式化一直是编程时永远绕不开的话题，Java和C的处理方式是在字符串中用<code>%s %d</code>作为占位符来进行字符串格式化，这种方法非常不友好，用户需要记住各种各样的占位符。<code>Python</code>的处理稍微高了那么一点点🤏，使用<code>{}</code>来进行占位，由于Python中将对象转化为字符串非常方便，所以用户处理起来也不是那么痛苦。现在，<code>Kotlin</code>彻底释放了用户，直接在字符串里面用<code>$</code>加上变量名的方式就可以了，<code>Kotlin</code>会帮你执行<code>toString</code>的操作，甚至还可以在里面执行表达式，只要用<code>${}</code>将表达式括起来即可。</p>
<h2 id="三-变量">三、变量</h2>
<p><code>Kotlin</code>声明变量很简单，看例子：</p>
<pre><code class="language-kotlin">fun variableFundamental() {
    var mutableValue: Int = 100
    println(mutableValue)
    mutableValue = 101
    println(mutableValue)
    val immutableValue = 10
    println(immutableValue)
}
</code></pre>
<p><code>val</code>修饰的变量为不可变的变量，注意这里的不可变，理解为它的引用不可变，如果你生命的是一个<code>ArrayList</code>，你还是可以自由地往里面添加元素。<code>var</code>修饰的变量是可变变量。</p>
<h2 id="四-类">四、类</h2>
<p>虽然<code>Kotlin</code>也是JVM系的语言，但是可以看到前一节的<code>main.kt</code>的三个函数不在类中，但是也可以编译运行。事实上，即使在<code>Java</code>中，静态函数也是顶级的函数，类名只是静态函数的命名空间（C++用户应该非常熟悉这个概念）。虽然如此，但是出了入口<code>main</code>函数，最好不要定义这种函数，就跟<code>Java</code>一样，用类来组织代码就好了。</p>
<p>在<code>Kotlin</code>中，类的定义非常的简洁，例如：</p>
<pre><code class="language-kotlin">class KotlinClass(param1: String, private val member1: Int = 10, val member2: String = &quot;&quot;)
</code></pre>
<p><code>Kotlin</code>通过<code>class</code>关键词来标记类，后面跟类名，这个十分好理解，后面的括号中包括的是类的构造函数的参数，如果参数被用<code>val</code>或者<code>var</code>修饰了，该参数就会自动成为类的成员变量。这样做的优点就是在<code>Kotlin</code>里面不再需要写<code>this.member1=member1</code>这样的废话了😄。这里的成员默认是<code>public</code>的，如果想要设置为<code>private</code>，只要在<code>val</code>前面加上访问权限修饰符<code>private</code>即可。<br>
传统的面向对象语言，不管是C++还是Java，都会提供一个叫构造函数重载的概念，即可以通过不同的参数来以不同的逻辑来构造对象。<code>Kotlin</code>里面没有重载这一说，<code>Kotlin</code>通过为构造函数提供默认参数实现了函数的重载，类似于前面的具名参数（<code>Python</code>中通过为<code>__init__</code>函数设置默认参数取得了同样的效果）。</p>
<p>下面扩充一下上面的类:</p>
<pre><code class="language-kotlin">class KotlinClass(param1: String, private val member1: Int = 10, var member2: String = &quot;&quot;) {
    companion object {
        fun staticFunc() {
            println(&quot;this static func&quot;)
        }
    }

    private var param1Receiver: String = param1

    private fun privateMemberFunc1() {
        println(member1)
    }

    fun publicMemberFunc() {
        println(member2)
    }
}
</code></pre>
<p><code>Kotlin</code>中没有<code>static</code>关键词，它使用<code>companion object</code>块来放置静态变量和函数，它的成员函数也类似<code>Java</code>，同样<code>public</code>为默认修饰符。</p>
<h2 id="五-总结">五、总结</h2>
<p>这一节简单叙述了<code>Kotlin</code>的变量声明、函数和类的定义，它的简洁和优雅一览无余，现在编写完整的<code>Kotlin</code>程序已经毫无问题了，但是想要进一步感受<code>Kotlin</code>的魅力，还需要继续探索。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[README]]></title>
        <id>https://jsrdzhk.github.io/post/readme/</id>
        <link href="https://jsrdzhk.github.io/post/readme/">
        </link>
        <updated>2021-02-18T08:16:20.000Z</updated>
        <content type="html"><![CDATA[<p>software developer,loler,crossfit,guitar player</p>
]]></content>
    </entry>
</feed>